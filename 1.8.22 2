// Online C compiler to run C program online
#include <stdio.h>
int fact(int);
int main() {
    int n;
    long int f;
    printf("enter number:");
    scanf("%d",&n);
    f=fact(n);
    printf("%ld",f);
}
int fact(int n)
{
    if(n==1)
    return 1;
    else
    return n*fact(n-1);
}

