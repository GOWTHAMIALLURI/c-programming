// Online C compiler to run C program online
#include <stdio.h>

int main() {
    char s[20];
    int i;
    gets(s);
    for(i=0;s[i]!='\0';i++)
    {
        if(s[i]=='a')
        {
        s[i]=s[i]-32;
        }
    }
puts(s);
    return 0;
}
